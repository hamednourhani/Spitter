DROP TABLE SPITTER;
DROP TABLE SPITTLE;
DROP TABLE FOLLOWEE;

CREATE TABLE SPITTER(
      ID       INTEGER NOT NULL AUTO_INCREMENT,
      USERNAME VARCHAR(30) NOT NULL,
      PASSWORD VARCHAR(30) NOT NULL,
      FULLNAME VARCHAR(30) NOT NULL,
      EMAIL    VARCHAR(50),
      PRIMARY KEY (ID)
);

CREATE TABLE SPITTLE(
      ID INTEGER NOT NULL AUTO_INCREMENT,
      SPITTER_ID INTEGER NOT NULL,
      TEXT VARCHAR(140) NOT NULL,
      POST_DATE VARCHAR(30) NOT NULL,
      PRIMARY KEY (ID)
);

CREATE TABLE FOLLOWEE(
      FOLLOWEE_ID INTEGER,
      SPITTER_ID INTEGER
);

INSERT INTO SPITTER (USERNAME, PASSWORD, FULLNAME) VALUES ('admin', 'admin1', 'administrator');
INSERT INTO SPITTER (USERNAME, PASSWORD, FULLNAME) VALUES ('admin2', 'admin2', 'admin2');
INSERT INTO SPITTLE (SPITTER_ID, TEXT, POST_DATE) VALUES (1, 'First spittle', 'Some date');